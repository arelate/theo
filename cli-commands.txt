# Decorators legend:
# $ - supports environmental variable value
# ^ - default property, value
# & - supports multiple values
# * - required value
# {} - placeholder values
# {^} - placeholder values, first value is default

backup-metadata

connect
    url
    username^
    password
    steam
    epic-games
    cookie
    reset

download
    id^*
    os&={operating-systems^}
    lang-code&={language-codes^}
    download-type&={download-types^}
    manual-url-filter&
    steam
    epic-games
    force

fetch-data
    id^*
    os={operating-systems^}
    steam
    epic-games

fix
    id*^
    os={operating-systems^}
    lang-code&={language-codes^}
    steam-appid
    revert

install
    id^
    os={operating-systems^}
    lang-code={language-codes^}
    download-type&={download-types^}
    keep-downloads
    steam
    epic-games
    no-steam-shortcut
    no-preset-launch-options
    env&
    verbose
    force

launch-options
    id^*
    os={operating-systems^}
    lang-code={language-codes^}
    exe
    arg&
    env&
    reset

list
    id^
    available-products
    installed
    launch-options
    steam-shortcuts
    tasks
    all-shortcut-keys
    os={operating-systems^}
    lang-code={language-codes^}
    steam
    epic-games
    update
    force

prefix
    id^*
    lang-code={language-codes^}
    exe
    arg&
    env&
    program={wine-programs}
    mod={prefix-mods}
    install-wine-binary={wine-binaries-codes}
    verbose
    force

preset-launch-options
    id^*
    os={operating-systems^}
    lang-code={language-codes^}

remove-downloads
    id^*
    os&={operating-systems^}
    lang-code&={language-codes^}
    download-type&={download-types^}

reveal
    id^
    os={operating-systems^}
    lang-code={language-codes^}
    installed
    downloads
    backups

run
    id^
    os={operating-systems^}
    lang-code={language-codes^}
    env&
    arg&
    task
    default-launcher
    work-dir
    proton-runtime=proton-ge,umu-proton
    steam-proton-runtime={steam-proton-runtimes}
    proton-options&=enable-wayland,prefer-sdl,disable-hidraw,enable-hdr,no-steaminput
    no-fix
    verbose
    force

setup-steamcmd
    force

setup-wine
    force

steam-shortcut
    id^*
    os={operating-systems^}
    lang-code={language-codes^}
    origin={origins}
    for-id
    header
    capsule
    hero
    logo
    icon
    pinned-position=BottomLeft,BottomCenter,CenterCenter,UpperCenter
    width-pct
    height-pct
    remove
    force

uninstall
    id^*
    os={operating-systems^}
    lang-code={language-codes^}
    purge
    verbose
    force

update
    id^
    all
    verbose
    force

validate
    id^*
    os&={operating-systems^}
    lang-code&={language-codes^}
    manual-url-filter&
    steam
    epic-games
    force

version